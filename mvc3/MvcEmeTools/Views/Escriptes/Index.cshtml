@model IEnumerable<Dominio.Entidades.Escripte>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Nome
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(@item.Nome, "Visualizar", new { id=item.IdSha1 })
        </td>
        <td>
            @Html.ActionLink("X", "Delete", new { id = item.IdSha1 })
        </td>
    </tr>
}

</table>
